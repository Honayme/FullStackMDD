// This file can be replaced during build using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// File replacements are specified in `angular.json`.

export const environment = {
  production: false, // Development mode indicator
};

/*
 * For easier debugging in dev mode, you can import the following file
 * to ignore zone-related error stack frames like `zone.run` or `zoneDelegate.invokeTask`.
 * This import should be commented out in production mode due to its performance impact.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
